# Data Directory

This public repository does not version generated dataset artifacts.

Running the build pipeline will create local subdirectories such as `raw/`, `processed/`, and `audits/` under this folder. Those artifacts are intentionally omitted from the tracked GitHub release to keep the repository focused on source code, configs, and manuscript materials.
